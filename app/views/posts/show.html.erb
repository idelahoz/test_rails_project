<h1><%= @post.title %></h1>

<p><%= @post.body %></p>

<%= link_to('Back',  posts_path ) %>
<%= link_to('Editar',  edit_post_path(@post) ) %>
<%= link_to("Delete Post", @post, method: "delete", data: { confirm: "Seguro que quieres borrar está mondá?"}) %>
